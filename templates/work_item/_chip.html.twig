<a href="{{ path('app_work_item_show', {key: work_item.key}) }}"
   class="Chip"
   title="{{ work_item.title }}"
>
    <span class="chip-label">{{ work_item.title }}</span>
    <code>{{ work_item.key }}</code>
</a>
